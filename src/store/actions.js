export const changeAction = (lang) => ({
    type: 'CHANGE_ACTION',
    lang
})

export const saveAmount = (amount) => ({
    type: 'SAVE_ACTION',
    amount  
})

export const sendInfo = (sendInfo) => ({
    type: 'SEND_INFO',
    sendInfo
})